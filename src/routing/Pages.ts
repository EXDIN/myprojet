enum Pages {
    Home = "/",
    Articles = "/articles",
    Registration = "/registration",
    Authorization = "/authorization",
    Comments = "/comments",
    Event = "/event",
    Undefined = "/*",
}

export default Pages;